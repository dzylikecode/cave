from setuptools import find_packages, setup


setup(
    name="demo",
    version="0.1.0",
    description="A simple engine demo package",
    packages=find_packages(),
    python_requires=">=3.8",
)
