name: py_embed
description: Embed CPython in Dart applications using native FFI bindings to the Python C API.
version: 0.0.6
repository: https://github.com/dzylikecode/cave/tree/main/packages/py_embed

platforms:
  # web:
  # android:
  # ios:
  linux:
  # macos:
  windows:

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  ffi: ^2.2.0
  meta: ^1.19.0
  path: ^1.9.1

dev_dependencies:
  ffigen: ^20.1.1
  lints: ^6.0.0
  test: ^1.25.6
