name: torch_dart
description: Dart bindings for PyTorch with a replaceable backend.
version: 0.0.1
repository: https://github.com/dzylikecode/cave/tree/main/packages/torch_dart

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  py_embed: ^0.0.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
